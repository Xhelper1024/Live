-----------------1.0.1版本-----------------
解决了以下的问题：
1，不需要每个房间都重复写一个JSP，所有的房间都是由一个JSP动态生成的
2，房间聊天只有本房间的观众能看到，刚开始是一个房间的消息所有房间都能看到，手动尴尬
3，将消息和表情区分开来，并且排除掉错误的表情代码，以原字符输出
4，用户聊天id问题，提供数据库供用户注册，不可修改用户名，不能重复用户名
5，主界面样式！美观了一万倍！
新增：
nginx-rtmp启动命令：nginx.exe -c conf\nginx-win-[rtmp].conf